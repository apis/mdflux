name: Release Build

on:
  release:
    types: [created]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install just
        uses: extractions/setup-just@v2

      - name: Download dependencies
        run: just deps

      - name: Fetch assets
        run: just fetch-assets

      - name: Build production binary
        run: |
          echo "Building for linux/amd64..."
          go build -trimpath -ldflags="-s -w" -o bin/mdflux-linux-amd64 ./cmd/mdflux

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mdflux-linux-amd64
          path: bin/mdflux-linux-amd64

      - name: Upload to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: bin/mdflux-linux-amd64

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install just
        uses: extractions/setup-just@v2

      - name: Download dependencies
        run: just deps

      - name: Fetch assets
        run: just fetch-assets

      - name: Build production binary
        run: |
          echo "Building for windows/amd64..."
          go build -trimpath -ldflags="-s -w" -o bin/mdflux-windows-amd64.exe ./cmd/mdflux

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: mdflux-windows-amd64
          path: bin/mdflux-windows-amd64.exe

      - name: Upload to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: bin/mdflux-windows-amd64.exe
